version: 2.1
jobs:
  build:
    executor: docker/docker
    environment:
      docker-username: kklim
      docker-password: 1loveprogramming
    steps:
      - setup_remote_docker
      - checkout
      - docker/check
      - docker/build:
          image: kklim/node-server


orbs:
  docker: circleci/docker@1.0.2
  
workflows:
  build:
    jobs:
      - docker/publish:
          image: kklim/node-server
